# @wl-universe/wl-auth

Shared `/site-auth` microservice (permissions). Port **3022**.

```powershell
npm run stack
# or
npm start -w @wl-universe/wl-auth
```

Auth: `Authorization: Bearer <WL_AUTH_API_KEY>` + `X-WL-Site`.
