name: QA wildcard bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap:
    uses: r2pen2/deploy-infra-actions/.github/workflows/reusable-qa-wildcard.yml@main
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
